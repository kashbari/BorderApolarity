import numpy as np

'''
Lambda = Highest Weight of irreducible representation
Mu = Weight of interest
'''
L = 
M = 
C = CartanMatrix(['A',len(L)])
det = int(round(np.linalg.det(np.array(C))))


def Killing(a,b):
	


def innprod(nu,eta):
	


def Freudenthal(M,L):
	if L == M;
		return 1
	else:
		d = [1]*len(L) #half sum of positive roots
		denom = innprod(L+d,L+d) - innprod(M+d,M+d)
		wtstr = [max(i,1) for i in L]
		
	
